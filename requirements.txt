# Runtime dependencies — pinned for reproducible installs across machines.
# Strategy:
#   - Bump a package: change its line here, then run a full smoke test / CI.
#   - For a byte-for-bit lock on a specific OS, after `pip install -r requirements.txt`:
#         pip freeze > requirements.freeze.txt
#     (Prefer generating freeze on the same OS you deploy to; wheels differ by platform.)
#
beautifulsoup4==4.14.3
django-cors-headers==4.9.0
# Adım 5.4: per-IP rate limiting on /api/v1/ask. Storage uses Django's CACHES
# (LocMemCache by default; switch to Redis for multi-worker deployments).
django-ratelimit==4.1.0
Django==5.2.13
gunicorn==25.3.0
httpx==0.28.1
lxml==6.1.0
numpy==1.26.4
playwright==1.59.0
psycopg2-binary==2.9.12
python-dotenv==1.2.2
requests==2.33.1
sentence-transformers==5.4.1
# transformers 5.x (5.7.0) ships a module-level type hint
# (``list[nn.Module]``) without importing ``nn``, which raises NameError
# at import time on Python 3.10. sentence-transformers 5.4.1 itself
# requires ``transformers>=4.41,<6``, so pinning to the 4.x series keeps
# us inside the supported range while avoiding the broken 5.x release.
transformers>=4.41,<5
whitenoise==6.12.0

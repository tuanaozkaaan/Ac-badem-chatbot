"""HTTP boundary for the chatbot app.

Versioned subpackages (`v1`, future `v2`, ...) host thin Django views, request/response
serializers, and authorization hooks. Domain logic lives under ``chatbot.services``.
"""

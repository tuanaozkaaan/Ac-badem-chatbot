# Developer / CI dependencies (install on top of runtime).
-r requirements.txt

pytest==9.0.3
pytest-django==4.12.0
# OpenAPI 3.x linter for tests/test_openapi_valid.py — pulls PyYAML.
openapi-spec-validator==0.8.4
PyYAML==6.0.3
